# CSV-Processor
A desktop tool built in Python that automates the conversion of IQMaps CSV exports into the format used in Carlson Survey. Eliminates manual reformatting by handling column cleanup, point renumbering, elevation calculation, feature normalisation, and group tagging. All in a few seconds.
